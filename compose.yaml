services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: perplexity-api-server:local
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
    ports:
      - "3000:3000"
    restart: unless-stopped
